# never-sleep-code-team

A graph workflow agent project using ADK 2.0.

This agent takes a coding task and generates code with an explanation.

## Development
- Run the agent locally: `agents-cli run "task description"` or run in the playground with `agents-cli playground`.
